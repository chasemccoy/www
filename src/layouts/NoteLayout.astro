---
import BaseLayout from './BaseLayout.astro';
import { getBlogroll } from '../utils/collections';

interface Props {
  title: string;
  excerpt?: string;
  url: string;
  featuredPosts?: any[];
  years?: string[];
}

const { title, excerpt, url, featuredPosts = [], years = [] } = Astro.props;
const blogroll = await getBlogroll();

let headerHtml = `<h1 class="font-header color-accent"><a href="${url}" class="unstyled">${title}</a></h1>`;
if (excerpt) {
  headerHtml += `<p class="lead color-caption">${excerpt}</p>`;
}
---
<BaseLayout
  title={title}
  templateClass="note"
  header={headerHtml}
  featuredPosts={featuredPosts}
  years={years}
  blogroll={blogroll}
>
  <section class="prose">
    <table-of-contents hidden>
      <h2>Table of Contents</h2>
    </table-of-contents>

    <article class="prose">
      <slot />
    </article>
  </section>
</BaseLayout>
