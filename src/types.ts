export interface LayoutProps {
  title?: string;
  description?: string;
}

export interface PostLink {
  permalink: string;
  title?: string;
}

export interface BlogrollItem {
  name: string;
  url: string;
}
